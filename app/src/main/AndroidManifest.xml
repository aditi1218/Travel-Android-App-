<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.travel">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/travel"
        android:label="travel"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".Main3Activity" />
        <activity android:name=".Main4Activity" />
        <activity android:name=".Developers" />
        <activity android:name=".background" />
        <activity android:name=".pm" />
        <activity android:name=".pcm" />
        <activity android:name=".vasu" />
        <activity android:name=".orchid" />
        <activity android:name=".rtrends" />
        <activity android:name=".gravity" />
        <activity android:name=".chunni" />
        <activity android:name=".pind" />
        <activity android:name=".yochina" />
        <activity android:name=".seventeen" />
        <activity android:name=".chef" />
        <activity android:name=".oliviya" />
        <activity android:name=".roti" />
        <activity android:name=".mchina" />
        <activity android:name=".nirvana" />
        <activity android:name=".zoo" />
        <activity android:name=".museum" />
        <activity android:name=".gghar" />
        <activity android:name=".funtasia" />
        <activity android:name=".buddha" />
        <activity android:name=".scenter" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"> </activity>

        <activity
            android:name=".nav_Drawer"
            android:label="@string/title_activity_nav__drawer"
            android:theme="@style/AppTheme.NoActionBar"> </activity>
    </application>

</manifest>